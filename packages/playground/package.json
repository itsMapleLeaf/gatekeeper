{
  "private": true,
  "scripts": {
    "dev": "nodemon -w ./ -w ../gatekeeper --transpile-only ./src/bot.ts",
    "dev-counter": "nodemon -w ./ -w ../gatekeeper --transpile-only ./src/counter-vanilla.ts",
    "dev-usage": "nodemon -r dotenv/config -w ./ -w ../gatekeeper --transpile-only ./src/usage.ts",
    "start": "ts-node --transpile-only ./src/bot"
  },
  "dependencies": {
    "@itsmapleleaf/gatekeeper": "workspace:*",
    "ts-node": "10.2.1",
    "discord.js": "13.2.0",
    "dotenv": "10.0.0"
  },
  "devDependencies": {
    "nodemon": "2.0.13",
    "@types/node": "16.10.3"
  }
}
